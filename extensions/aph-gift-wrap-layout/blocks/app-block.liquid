{% if request.page_type == 'product' %}
  {% render "app_snippet" %}
{% endif %}

{% if request.page_type == 'cart' %}
  <script>
    fetch(window.Shopify.routes.root + 'cart.js')
      .then(response => response.json())
      .then( data => { 
        console.log('contewnte ', data);
      });
  </script>
{% endif %}

{% schema %}
  {
    "name": "APH Gift Wrap Settings",
    "target": "section",
    "stylesheet": "aph-app.css",
    "javascript": "aph-app.js",
    "settings": [
        { "label": "Color", "id": "color", "type": "color", "default": "#000000" },
        { "label": "Gift Product", "id": "product", "type": "product" }
    ]
  }
{% endschema %}
